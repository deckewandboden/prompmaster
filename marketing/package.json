{
  "name": "promptmaster-commercial-marketing",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite --config vite.marketing.config.js",
    "build": "vite build --config vite.marketing.config.js && node scripts/finalize.mjs",
    "start": "vite preview --config vite.marketing.config.js --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "lint": "oxlint src scripts tests",
    "format": "oxfmt"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "oxfmt": "0.61.0",
    "oxlint": "1.76.0",
    "vite": "8.2.2"
  },
  "type": "module"
}
